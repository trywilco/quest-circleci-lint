id: circleci-configure
title: CircleCI configure
description: "We lately saw many changes that broke our unit tests in the frontend, so we want to run those tests with every change using CircleCI to make sure we don't break anything."
summary: "Summary"
level: beginner
steps:
  - circleci-configure-template
  - circleci-task-status
  - circleci-check-error
  - circleci-fix-error
duration: 1
skills:
  - circleci
questDependency: docker_localsetup
slogan: Starting slow
resources:
  - name: circleci
